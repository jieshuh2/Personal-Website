import { createTheme } from "@material-ui/core";

export const theme = createTheme({
    palette: {
      primary: {
        main: "#64ffda",
        dark: "#14cba8",
        light: "#9effff"
      },
      secondary: {
        main: "#00e5ff",
        light: "#6effff",
        dark: "#00b2cc"
      },
    },
});